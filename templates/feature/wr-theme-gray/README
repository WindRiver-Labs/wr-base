This template sets the default theme to windriver-gray
for XFCE desktop environment.

It is supported for x86 and x86-64 targets only.

To use this template, you need to add

--with-layer=common --with-template=feature/xfce,feature/wr-theme-gray

as a configure option.
